ext {
    versionConfig = [
            compileSdkVersion    : 31,
            minSdkVersion        : 19,
            targetSdkVersion     : 31,
            versionCode          : 1,
            versionName          : "1.0"
    ]

    androidxConfig = [
            appcompat           : "1.2.0",
            constraintlayout    : "2.0.4",
            junit               : "4.13.2",
            ext                 : "1.1.2",
            espresso_core       : "3.3.0",
            material            : "1.3.0"
    ]

    dependConfig = [

    ]
}